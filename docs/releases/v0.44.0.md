# v0.44.0 Release Notes

v0.44.0 turns the v0.43.0 Sector Evidence Board into a stronger served-dashboard quality gate. The release verifies that the button payload rendered inside the dashboard can update `review_action_state.json` through the same API path used by served dashboards.

## Added

- Regression test for the rendered sector evidence-board `補證並標記完成` button payload.
- State-write verification that confirms the button payload can mark a source-audit blocker `done` with:
  - `note`,
  - `reviewer`,
  - `evidence_url`.
- Post-write dashboard verification that the stock-level evidence row can move to `證據可交付` after the evidence file exists and the state is updated.

## Changed

- README, usage workflow, changelog, release notes, and package version now target `v0.44.0`.

## Non-Advice Boundary

This release improves workflow verification only. It does not add investment recommendations, sector attractiveness rankings, buy/sell/hold decisions, target prices, or allocation guidance.

## Verification

```powershell
python -m unittest tests.test_dashboard_server -v
python -m unittest discover -s tests -v
python -m taiwan_stock_analysis.cli doctor release --version 0.44.0
```
