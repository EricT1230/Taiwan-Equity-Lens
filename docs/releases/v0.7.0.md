# v0.7.0 - Memo Intelligence Upgrade

v0.7.0 upgrades deterministic research memos into stronger review drafts for single-stock research.

## Highlights

- Executive summary, key observations, catalysts, risks, open questions, and grouped next research actions.
- Markdown and HTML memo outputs use the same richer research structure.
- Risk and follow-up sections respond to reliability warnings, diagnostics, and valuation completeness.

Memos remain research workflow support only. They do not provide investment advice, portfolio allocation guidance, or recommendation labels.

## Example

```powershell
python -m taiwan_stock_analysis.cli 2330 --company-name TSMC --output-dir memo-dist
python -m taiwan_stock_analysis.cli memo memo-dist/2330_raw_data.json --output memos/2330_memo.md
```

## Verify

```powershell
$env:PYTHONPATH='src'; python -m unittest discover -s tests -v
```
