# v0.23.0 Quickstart Onboarding Implementation Plan

> **For agentic workers:** This is a docs-only onboarding release. Do not add new CLI commands or behavior changes.

**Goal:** Make the complete offline demo and first review-action workflow easy to find and run.

**Architecture:** Update top-level and workflow documentation with copyable commands, expected output paths, and a short review-action follow-up path. Keep release metadata consistent with the existing release doctor.

**Tech Stack:** Markdown docs, existing release doctor, existing test suite.

---

## File Structure

- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `examples/README.md`
- Modify: `pyproject.toml`
- Modify: `CHANGELOG.md`
- Modify: `tests/test_cli.py`
- Create: `docs/releases/v0.23.0.md`

---

## Task 1: Add Quickstart Docs

- [ ] Add concise offline demo command near the top of `README.md`.
- [ ] Add dashboard path and expected output surfaces.
- [ ] Add review-action follow-up commands.
- [ ] Add "Fastest local demo" guidance to `docs/usage-workflow.md`.
- [ ] Add compact full-demo guidance to `examples/README.md`.
- [ ] Commit quickstart docs.

## Task 2: Release Metadata

- [ ] Bump `pyproject.toml` to `0.23.0`.
- [ ] Update README version badge and release links.
- [ ] Add `CHANGELOG.md` entry for `v0.23.0`.
- [ ] Create `docs/releases/v0.23.0.md`.
- [ ] Update release doctor CLI test to `0.23.0`.
- [ ] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.23.0`.
- [ ] Commit release metadata.

## Task 3: Final Verification

- [ ] Run `python -m taiwan_stock_analysis.cli research run examples/research.csv --fixture-root examples/fixtures --output-dir .tmp-cli-test/v0.23-quickstart-demo --offline-prices`.
- [ ] Run quickstart review-action commands against the demo output.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.23.0`.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.23.0`.
