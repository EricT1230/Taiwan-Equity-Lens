# v0.11.0 Offline Demo Kit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a deterministic offline demo path that lets new users run the full research workflow from committed synthetic fixtures.

**Architecture:** Commit synthetic fixture HTML under `examples/fixtures`, add a smoke test that runs the public demo command, then update docs and release metadata to make the offline demo the primary first-run path.

**Tech Stack:** Python standard library, existing argparse CLI, existing workflow/research/memo/pack modules, `unittest`.

---

## File Structure

- Create: `examples/fixtures/2330/IS_YEAR.html`
- Create: `examples/fixtures/2330/BS_YEAR.html`
- Create: `examples/fixtures/2330/CF_YEAR.html`
- Create: `examples/fixtures/2303/IS_YEAR.html`
- Create: `examples/fixtures/2303/BS_YEAR.html`
- Create: `examples/fixtures/2303/CF_YEAR.html`
- Create: `examples/fixtures/2454/IS_YEAR.html`
- Create: `examples/fixtures/2454/BS_YEAR.html`
- Create: `examples/fixtures/2454/CF_YEAR.html`
- Modify: `tests/test_cli.py`
- Modify: `README.md`
- Modify: `examples/README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Modify: `pyproject.toml`
- Create: `docs/releases/v0.11.0.md`

---

### Task 1: Add Synthetic Offline Fixtures

**Files:**
- Create: all files under `examples/fixtures/{2330,2303,2454}/`

- [ ] **Step 1: Create fixture directories**

Run:

```powershell
New-Item -ItemType Directory -Force examples/fixtures/2330,examples/fixtures/2303,examples/fixtures/2454
```

Expected: directories exist.

- [ ] **Step 2: Add synthetic income statement fixtures**

Each `IS_YEAR.html` should use this structure, with stock-specific numeric values:

```html
<!-- Synthetic fixture for Taiwan Equity Lens tests and demos. -->
<html><body>
  <table></table><table></table><table></table><table></table><table></table><table></table>
  <table>
    <tr><th>項目</th><th>2024</th><th>%</th><th>2023</th><th>%</th></tr>
    <tr><td>營業收入合計</td><td>1000</td><td>100</td><td>900</td><td>100</td></tr>
    <tr><td>營業毛利（毛損）</td><td>520</td><td>52</td><td>450</td><td>50</td></tr>
    <tr><td>營業利益（損失）</td><td>360</td><td>36</td><td>300</td><td>33.3</td></tr>
    <tr><td>稅後淨利</td><td>300</td><td>30</td><td>240</td><td>26.7</td></tr>
    <tr><td>每股稅後盈餘(元)</td><td>12</td><td></td><td>10</td><td></td></tr>
  </table>
</body></html>
```

Use different values per stock so comparison output is meaningful:

- `2330`: stronger margins and EPS
- `2303`: moderate margins and EPS
- `2454`: strong revenue and profitability, but different balance-sheet profile

- [ ] **Step 3: Add synthetic balance sheet fixtures**

Each `BS_YEAR.html` should include:

```html
<!-- Synthetic fixture for Taiwan Equity Lens tests and demos. -->
<html><body>
  <table></table><table></table><table></table><table></table><table></table><table></table>
  <table>
    <tr><th>項目</th><th>2024</th><th>%</th><th>2023</th><th>%</th></tr>
    <tr><td>流動資產合計</td><td>700</td><td></td><td>620</td><td></td></tr>
    <tr><td>流動負債合計</td><td>320</td><td></td><td>300</td><td></td></tr>
    <tr><td>負債總額</td><td>520</td><td></td><td>500</td><td></td></tr>
    <tr><td>資產總額</td><td>1500</td><td></td><td>1380</td><td></td></tr>
    <tr><td>股東權益總額</td><td>980</td><td></td><td>880</td><td></td></tr>
  </table>
</body></html>
```

- [ ] **Step 4: Add synthetic cash-flow fixtures**

Each `CF_YEAR.html` should include:

```html
<!-- Synthetic fixture for Taiwan Equity Lens tests and demos. -->
<html><body>
  <table></table><table></table><table></table><table></table><table></table><table></table>
  <table>
    <tr><th>項目</th><th>2024</th><th>%</th><th>2023</th><th>%</th></tr>
    <tr><td>營業活動之淨現金流入（出）</td><td>340</td><td></td><td>280</td><td></td></tr>
    <tr><td>固定資產（增加）減少</td><td>-90</td><td></td><td>-80</td><td></td></tr>
    <tr><td>發放現金股利</td><td>-120</td><td></td><td>-100</td><td></td></tr>
  </table>
</body></html>
```

- [ ] **Step 5: Verify fixture parser compatibility**

Run:

```powershell
python -m taiwan_stock_analysis.cli 2330 --company-name TSMC --fixture examples/fixtures/2330 --output-dir .tmp-cli-test/offline-fixture-single
```

Expected:

- `.tmp-cli-test/offline-fixture-single/2330_raw_data.json` exists
- `.tmp-cli-test/offline-fixture-single/2330_analysis.html` exists

- [ ] **Step 6: Commit**

```powershell
git add examples/fixtures
git commit -m "testdata: add synthetic offline demo fixtures"
```

---

### Task 2: Add Offline Demo Smoke Test

**Files:**
- Modify: `tests/test_cli.py`

- [ ] **Step 1: Write failing smoke test**

Add to `CliTests`:

```python
    def test_main_research_run_examples_offline_demo_writes_full_outputs(self):
        output_dir = Path(".tmp-cli-test/offline-demo-dist")

        exit_code = main([
            "research",
            "run",
            "examples/research.csv",
            "--fixture-root",
            "examples/fixtures",
            "--output-dir",
            str(output_dir),
            "--offline-prices",
        ])

        self.assertEqual(exit_code, 0)
        self.assertTrue((output_dir / "dashboard.html").exists())
        self.assertTrue((output_dir / "workflow_summary.json").exists())
        self.assertTrue((output_dir / "research_summary.json").exists())
        self.assertTrue((output_dir / "memos" / "memo_summary.json").exists())
        self.assertTrue((output_dir / "packs" / "pack_summary.json").exists())
        self.assertTrue((output_dir / "comparison" / "comparison.json").exists())
        self.assertTrue((output_dir / "comparison" / "comparison.html").exists())
```

- [ ] **Step 2: Run test to verify it passes with fixtures**

Run:

```powershell
python -m unittest tests.test_cli.CliTests.test_main_research_run_examples_offline_demo_writes_full_outputs -v
```

Expected: PASS after Task 1 fixtures exist.

- [ ] **Step 3: Commit**

```powershell
git add tests/test_cli.py
git commit -m "test: cover offline example demo workflow"
```

---

### Task 3: Document Offline Demo Path

**Files:**
- Modify: `README.md`
- Modify: `examples/README.md`
- Modify: `docs/usage-workflow.md`

- [ ] **Step 1: Update README one-command demo**

Change the demo command to:

```powershell
python -m taiwan_stock_analysis.cli research run examples/research.csv --fixture-root examples/fixtures --output-dir demo-dist --offline-prices
```

Change the open path to:

```text
demo-dist/dashboard.html
```

Add `examples/fixtures/` to Example Files.

- [ ] **Step 2: Update examples README**

Make the first demo section use:

```powershell
python -m taiwan_stock_analysis.cli research run examples/research.csv --fixture-root examples/fixtures --output-dir demo-dist --offline-prices
```

Add a short note:

```markdown
`examples/fixtures/` contains synthetic financial-statement HTML for offline demos. It is not source data and should not be used as real company data.
```

- [ ] **Step 3: Update usage workflow**

In the fixture section, replace the wording that says fixtures are intentionally not shipped. Explain:

- `examples/fixtures` is shipped for offline demos.
- Real research can still use a user-maintained fixture root.
- Live commands remain available.

- [ ] **Step 4: Run release doctor for current version**

Run:

```powershell
python -m taiwan_stock_analysis.cli doctor release
```

Expected: PASS for current metadata.

- [ ] **Step 5: Commit**

```powershell
git add README.md examples/README.md docs/usage-workflow.md
git commit -m "docs: make offline demo the primary quickstart"
```

---

### Task 4: Release v0.11.0

**Files:**
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.11.0.md`

- [ ] **Step 1: Bump version**

In `pyproject.toml`:

```toml
version = "0.11.0"
```

Update README badge to `v0.11.0`.

- [ ] **Step 2: Add changelog entry**

Add to top of `CHANGELOG.md`:

```markdown
## v0.11.0 - 2026-05-14

Offline Demo Kit release.

### Added

- Synthetic example financial-statement fixtures for fully offline demos.
- Offline research workflow demo smoke test.
- v0.11.0 release notes.

### Changed

- README and examples now use the offline research workflow as the primary demo path.
- Package version is now `0.11.0`.

### Quality

- The public offline demo command is covered by the test suite.
```

- [ ] **Step 3: Add release notes**

Create `docs/releases/v0.11.0.md`:

```markdown
# v0.11.0 - Offline Demo Kit

v0.11.0 adds synthetic fixtures and a fully offline demo path for first-time users.

## Highlights

- New `examples/fixtures/` synthetic financial-statement fixtures.
- README demo now runs without live financial-site fetches.
- The offline research workflow produces dashboard, workflow summary, research summary, memos, packs, and comparison outputs.
- The demo command is covered by the test suite.

## Verify

```powershell
python -m taiwan_stock_analysis.cli research run examples/research.csv --fixture-root examples/fixtures --output-dir demo-dist --offline-prices
python -m taiwan_stock_analysis.cli doctor release --version 0.11.0
python -m unittest discover -s tests -v
$env:UV_NATIVE_TLS='true'
uv run --python 3.10 python -m unittest discover -s tests -v
```
```

- [ ] **Step 4: Add README release link**

Add `v0.11.0` release notes above `v0.10.0` in README Documentation.

- [ ] **Step 5: Run release doctor**

Run:

```powershell
python -m taiwan_stock_analysis.cli doctor release --version 0.11.0
```

Expected: `Release readiness OK`.

- [ ] **Step 6: Run full tests**

Run:

```powershell
python -m unittest discover -s tests -v
$env:UV_NATIVE_TLS='true'
uv run --python 3.10 python -m unittest discover -s tests -v
```

Expected: all tests pass.

- [ ] **Step 7: Commit release**

```powershell
git add pyproject.toml README.md CHANGELOG.md docs/releases/v0.11.0.md
git commit -m "docs: document v0.11.0 offline demo kit"
```

---

### Task 5: Final Verification And Tag

**Files:**
- No source edits expected.

- [ ] **Step 1: Verify working tree**

Run:

```powershell
git status --short --branch
```

Expected: clean working tree on `main`.

- [ ] **Step 2: Push main**

Run:

```powershell
git push origin main
```

Expected: push succeeds.

- [ ] **Step 3: Create and push tag**

Run:

```powershell
git tag -a v0.11.0 -m "v0.11.0"
git push origin v0.11.0
```

Expected: tag push succeeds.

- [ ] **Step 4: Verify GitHub Actions**

Confirm `main` and `v0.11.0` runs pass for:

- `unittest (3.10)`
- `unittest (3.11)`
- `unittest (3.12)`

## Plan Self-Review

- Spec coverage: fixture delivery, smoke test, docs, release metadata, and CI verification are covered.
- Placeholder scan: no placeholder markers remain.
- Type consistency: all command names and paths match the design spec.
