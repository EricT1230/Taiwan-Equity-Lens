# v0.22.0 Review Action State Backup List Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep this release limited to a read-only backup inventory command; do not add latest-backup restore, cleanup, or dashboard controls.

**Goal:** Add a CLI command that lists backup files available for a review-action state file.

**Architecture:** Add a backup-list helper in `review_action_state.py` that finds `{state_path.name}.bak-*` files, parses timestamp suffixes when possible, and returns deterministic newest-first rows. Integrate it into `research action backups STATE_PATH [--json]` with text and JSON output.

**Tech Stack:** Python standard library, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/review_action_state.py`
- Modify: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.22.0.md`

---

## Task 1: Add Backup List Helper

- [x] Add `list_review_action_state_backups(path)`.
- [x] Match only `{path.name}.bak-*` files in `path.parent`.
- [x] Parse timestamp suffixes shaped `YYYYMMDDTHHMMSSZ`.
- [x] Use `created_at: unknown` for unparseable suffixes.
- [x] Include `size` and `path` for each row.
- [x] Sort parsed timestamps newest first, then unknown timestamps, then deterministic path order.
- [x] Skip entries that cannot be statted.
- [x] Add unit tests for matching, ignoring unrelated files, sorting, collisions, missing parent, and unknown timestamps.
- [x] Run `python -m unittest tests.test_review_action_state -v`.
- [x] Commit backup list helper.

## Task 2: Add CLI Backups Command

- [ ] Add `research action backups STATE_PATH [--json]`.
- [ ] Print default tab-separated text header `created_at\tsize\tpath`.
- [ ] Print no data rows when no backups exist.
- [ ] Print deterministic JSON object with `state_path` and `backups`.
- [ ] Hide any internal sort-only fields from CLI output.
- [ ] Add CLI tests for text output, empty output, and JSON output.
- [ ] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [ ] Commit CLI backup-list integration.

## Task 3: Documentation and Release Metadata

- [ ] Bump `pyproject.toml` to `0.22.0`.
- [ ] Update README version badge and feature list.
- [ ] Update `docs/usage-workflow.md` with backup-list behavior.
- [ ] Add `CHANGELOG.md` entry for `v0.22.0`.
- [ ] Create `docs/releases/v0.22.0.md`.
- [ ] Update release doctor CLI test to `0.22.0`.
- [ ] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.22.0`.
- [ ] Commit docs and release metadata.

## Task 4: Final Verification

- [ ] Run focused state/CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.22.0`.
- [ ] Run demo workflow showing backup list text and JSON output.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.22.0`.
