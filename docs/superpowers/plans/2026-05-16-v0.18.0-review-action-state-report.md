# v0.18.0 Review Action State Report Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep this release read-only: report state health, do not mutate or prune state.

**Goal:** Add CLI and dashboard audit reporting for persisted review-action state.

**Architecture:** Extend `review_action_state.py` with report helpers that compare current generated queue keys against normalized sidecar state. Add `research action report` CLI output and a compact dashboard state-health line. Keep the v0.16 sidecar schema unchanged.

**Tech Stack:** Python standard library, static dashboard HTML, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/review_action_state.py`
- Modify: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `src/taiwan_stock_analysis/dashboard.py`
- Modify: `tests/test_dashboard.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.18.0.md`

---

## Task 1: Add State Report Helpers

- [x] Add helper to extract current action keys from generated queue.
- [x] Add helper to build stale state rows from normalized state entries not present in current keys.
- [x] Add helper to compute max `updated_at`.
- [x] Add `build_review_action_state_report(queue, state, next_open_limit=5)`.
- [x] Include `total_actions`, `by_status`, `stale_state`, `stale_count`, `last_updated`, and `next_open`.
- [x] Add unit tests for status counts, stale state, last updated, next open order, missing state, and malformed state.
- [x] Run `python -m unittest tests.test_review_action_state -v`.
- [x] Commit state report helpers.

## Task 2: Add CLI Report

- [x] Add `research action report RESEARCH_SUMMARY --state STATE_PATH [--next-open-limit N]`.
- [x] Reuse default sibling state path when `--state` is omitted.
- [x] Print warning for invalid state JSON and continue with empty state.
- [x] Print deterministic text sections for totals, status counts, stale state, last updated, and next open.
- [x] Add CLI tests for explicit/default state path and invalid state warning.
- [x] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [x] Commit CLI report.

## Task 3: Add Dashboard State Health

- [ ] Build a state report for each Review Actions section.
- [ ] Render compact health badges for state counts, stale state count, and last updated.
- [ ] Keep invalid state warning behavior unchanged.
- [ ] Do not render stale entries in the main action table.
- [ ] Add dashboard tests for health badges, stale count, last updated, and missing/invalid state behavior.
- [ ] Run `python -m unittest tests.test_dashboard tests.test_review_action_state -v`.
- [ ] Commit dashboard health.

## Task 4: Documentation and Release Metadata

- [ ] Bump `pyproject.toml` to `0.18.0`.
- [ ] Update README version badge and feature list.
- [ ] Update `docs/usage-workflow.md` with `research action report`.
- [ ] Add `CHANGELOG.md` entry for `v0.18.0`.
- [ ] Create `docs/releases/v0.18.0.md`.
- [ ] Update release doctor CLI test to `0.18.0`.
- [ ] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.18.0`.
- [ ] Commit docs and release metadata.

## Task 5: Final Verification

- [ ] Run focused state/CLI/dashboard tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.18.0`.
- [ ] Run demo research workflow, set one state entry, add one stale entry, and verify CLI report plus dashboard health.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.18.0`.
