# v0.19.0 Prune Stale Review Action State Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep pruning explicit: dry-run by default, write only with `--write`.

**Goal:** Add a safe CLI cleanup path for stale `review_action_state.json` entries reported by v0.18.0.

**Architecture:** Extend `review_action_state.py` with a pruning helper that removes normalized state entries not present in the current generated queue. Add `research action prune-stale` CLI output with dry-run and write modes. Keep the state schema unchanged.

**Tech Stack:** Python standard library, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/review_action_state.py`
- Modify: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.19.0.md`

---

## Task 1: Add State Pruning Helper

- [ ] Add `prune_stale_review_action_state(action_queue, state)`.
- [ ] Normalize incoming state before pruning.
- [ ] Preserve only state entries whose keys exist in current review-action keys.
- [ ] Return both the pruned state and stale rows for reporting.
- [ ] Do not mutate the input state.
- [ ] Add unit tests for stale removal, no-stale behavior, missing state, malformed state, and input immutability.
- [ ] Run `python -m unittest tests.test_review_action_state -v`.
- [ ] Commit state pruning helper.

## Task 2: Add CLI Prune Command

- [ ] Add `research action prune-stale RESEARCH_SUMMARY --state STATE_PATH [--write]`.
- [ ] Reuse default sibling state path when `--state` is omitted.
- [ ] Dry-run by default and print stale rows without writing.
- [ ] With `--write`, rewrite the state file only when it already exists and loaded successfully.
- [ ] Return non-zero on invalid state JSON and do not write.
- [ ] Add CLI tests for dry-run, write, default state path, no stale entries, invalid state JSON, and missing state file.
- [ ] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [ ] Commit CLI prune command.

## Task 3: Documentation and Release Metadata

- [ ] Bump `pyproject.toml` to `0.19.0`.
- [ ] Update README version badge and feature list.
- [ ] Update `docs/usage-workflow.md` with `research action prune-stale`.
- [ ] Add `CHANGELOG.md` entry for `v0.19.0`.
- [ ] Create `docs/releases/v0.19.0.md`.
- [ ] Update release doctor CLI test to `0.19.0`.
- [ ] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.19.0`.
- [ ] Commit docs and release metadata.

## Task 4: Final Verification

- [ ] Run focused state/CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.19.0`.
- [ ] Run demo research workflow, set one state entry, add one stale entry, dry-run prune, write prune, and verify report plus state file.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.19.0`.
