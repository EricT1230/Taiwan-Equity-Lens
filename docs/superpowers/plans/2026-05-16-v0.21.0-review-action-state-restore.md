# v0.21.0 Review Action State Restore Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep this release limited to restoring explicit review-action state backup files; do not add dashboard controls, latest-backup selection, or cleanup policy.

**Goal:** Add a safe CLI restore command for `review_action_state.json.bak-*` files.

**Architecture:** Add a restore helper in `review_action_state.py` that validates the selected backup, validates any existing current state, backs up the current state before overwrite, and copies backup bytes to the target path. Integrate it into `research action restore` and document the workflow.

**Tech Stack:** Python standard library, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/review_action_state.py`
- Modify: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.21.0.md`

---

## Task 1: Add Restore Helper

- [x] Add `restore_review_action_state(path, backup_path)`.
- [x] Return `(path, current_backup_path)`.
- [x] Validate `backup_path` exists before writing.
- [x] Validate `backup_path` with `load_review_action_state`.
- [x] Validate existing `path` with `load_review_action_state` when present.
- [x] Back up existing valid `path` before overwrite.
- [x] Preserve backup file bytes exactly when restoring.
- [x] Add unit tests for byte preservation, missing target, current backup creation, missing backup, invalid backup, and invalid current state.
- [x] Run `python -m unittest tests.test_review_action_state -v`.
- [x] Commit restore helper.

## Task 2: Add CLI Restore Command

- [x] Add `research action restore STATE_PATH BACKUP_PATH`.
- [x] Call `restore_review_action_state`.
- [x] Print `Backup review action state: PATH` when current state is backed up.
- [x] Print `Restored review action state: PATH` on success.
- [x] Catch `ValueError`, print `Warning: ...`, and return `1`.
- [x] Add CLI tests for successful restore, missing target restore, invalid backup, and invalid current state.
- [x] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [x] Commit CLI restore integration.

## Task 3: Documentation and Release Metadata

- [x] Bump `pyproject.toml` to `0.21.0`.
- [x] Update README version badge and feature list.
- [x] Update `docs/usage-workflow.md` with restore behavior.
- [x] Add `CHANGELOG.md` entry for `v0.21.0`.
- [x] Create `docs/releases/v0.21.0.md`.
- [x] Update release doctor CLI test to `0.21.0`.
- [x] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.21.0`.
- [x] Commit docs and release metadata.

## Task 4: Final Verification

- [ ] Run focused state/CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.21.0`.
- [ ] Run demo workflow showing backup creation and restore.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.21.0`.
