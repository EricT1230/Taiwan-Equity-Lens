# v0.16.0 Review Action Completion Persistence Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep generated research summaries deterministic; persist manual review state only in `review_action_state.json`.

**Goal:** Persist review-action completion decisions across regenerated research outputs using a local sidecar state file.

**Architecture:** Add a focused `review_action_state.py` module for loading, writing, and applying persisted state. Extend CLI with `research action list` and `research action set`. Extend dashboard discovery/rendering to overlay sibling state files while preserving static, dependency-free HTML.

**Tech Stack:** Python standard library, `unittest`, existing static dashboard HTML/CSS/JavaScript, existing release doctor.

---

## File Structure

- Create: `src/taiwan_stock_analysis/review_action_state.py`
- Create: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `src/taiwan_stock_analysis/dashboard.py`
- Modify: `tests/test_dashboard.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.16.0.md`

---

## Task 1: Add Review Action State Module

- [x] Create `review_action_state.py`.
- [x] Define allowed statuses: `open`, `done`, `deferred`, `ignored`.
- [x] Add stable key helper: `<stock_id>:<action_id>`.
- [x] Add load helper that returns normalized state plus warning text for invalid JSON.
- [x] Add write/update helper that writes deterministic JSON with sorted keys.
- [x] Add queue overlay helper that copies queue entries and adds each action's persisted status and note.
- [x] Add summary helper for counts by status.
- [x] Add `tests/test_review_action_state.py`.
- [x] Run `python -m unittest tests.test_review_action_state -v`.
- [x] Commit state module and tests.

## Task 2: Add CLI State Management

- [x] Add `research action` nested parser.
- [x] Add `research action set STATE_PATH STOCK_ID ACTION_ID --status STATUS [--note NOTE]`.
- [x] Add `research action list RESEARCH_SUMMARY --state STATE_PATH`.
- [x] Make `set` validate statuses through argparse choices.
- [x] Make `list` print deterministic rows: stock, priority, status, severity, category, action ID, message.
- [x] Add CLI tests for set/list and invalid status parser behavior.
- [x] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [x] Commit CLI work.

## Task 3: Overlay State in Dashboard

- [x] Extend dashboard discovery so research summaries can carry sibling review state metadata.
- [x] When a `research_summary.json` sibling `review_action_state.json` exists, load it and overlay statuses.
- [x] If state JSON is invalid, keep actions visible as open and render a warning.
- [x] Add Review Actions `status` column.
- [x] Add `data-status` to rows and include status in search metadata.
- [x] Add status filter to existing local filter bar.
- [x] Add state counts badge.
- [x] Update JavaScript filtering for `status` with existing AND semantics.
- [x] Add dashboard tests for status overlay, status filter, warning, escaping, and no-state default open behavior.
- [x] Run `python -m unittest tests.test_dashboard tests.test_review_action_state -v`.
- [x] Commit dashboard work.

## Task 4: Documentation and Release Metadata

- [x] Bump `pyproject.toml` to `0.16.0`.
- [x] Update README version badge and feature list.
- [x] Update `docs/usage-workflow.md` with sidecar state commands.
- [x] Add `CHANGELOG.md` entry for `v0.16.0`.
- [x] Create `docs/releases/v0.16.0.md`.
- [x] Update release doctor CLI test to `0.16.0`.
- [x] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.16.0`.
- [x] Commit docs and release metadata.

## Task 5: Final Verification

- [ ] Run focused state/dashboard/CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.16.0`.
- [ ] Run demo research workflow and create/update a `review_action_state.json` entry.
- [ ] Regenerate dashboard and verify status column/filter/state counts appear.
- [ ] Confirm `git status --short --branch` is clean.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.16.0`.
