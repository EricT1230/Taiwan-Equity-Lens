# v0.20.0 Review Action State Backups Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep this release limited to write-before-backup behavior; do not add restore or retention features.

**Goal:** Create a timestamped backup before CLI commands rewrite an existing valid `review_action_state.json`.

**Architecture:** Add a backup helper in `review_action_state.py` that copies original bytes to a unique timestamped backup file. Integrate it into `research action set` and `research action prune-stale --write`, and surface backup paths in CLI output.

**Tech Stack:** Python standard library, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/review_action_state.py`
- Modify: `tests/test_review_action_state.py`
- Modify: `src/taiwan_stock_analysis/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.20.0.md`

---

## Task 1: Add Backup Helper

- [x] Add `backup_review_action_state(path, timestamp=None)`.
- [x] Use UTC timestamp suffix `review_action_state.json.bak-YYYYMMDDTHHMMSSZ`.
- [x] Preserve original file bytes exactly.
- [x] Return `None` when the state file does not exist.
- [x] Avoid overwriting existing backup names with numeric suffixes.
- [x] Add unit tests for naming, byte preservation, missing file, and collision handling.
- [x] Run `python -m unittest tests.test_review_action_state -v`.
- [x] Commit backup helper.

## Task 2: Integrate CLI Write Backups

- [x] Make `research action set` create a backup before writing an existing valid state file.
- [x] Make `research action prune-stale --write` create a backup before writing an existing valid state file.
- [x] Print `Backup review action state: PATH` when a backup is created.
- [x] Do not create backups for missing state files.
- [x] Do not create backups for invalid state JSON.
- [x] Ensure backup creation failure aborts the write.
- [x] Add CLI tests for set backup, prune backup, no-backup missing state, invalid state, and pre-write backup content.
- [x] Run `python -m unittest tests.test_cli tests.test_review_action_state -v`.
- [x] Commit CLI backup integration.

## Task 3: Documentation and Release Metadata

- [x] Bump `pyproject.toml` to `0.20.0`.
- [x] Update README version badge and feature list.
- [x] Update `docs/usage-workflow.md` with backup behavior.
- [x] Add `CHANGELOG.md` entry for `v0.20.0`.
- [x] Create `docs/releases/v0.20.0.md`.
- [x] Update release doctor CLI test to `0.20.0`.
- [x] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.20.0`.
- [x] Commit docs and release metadata.

## Task 4: Final Verification

- [ ] Run focused state/CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.20.0`.
- [ ] Run demo workflow showing backup creation before `set` and `prune-stale --write`.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.20.0`.
