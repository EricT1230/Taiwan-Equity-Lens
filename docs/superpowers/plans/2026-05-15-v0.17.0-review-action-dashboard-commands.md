# v0.17.0 Review Action Dashboard Commands Implementation Plan

> **For agentic workers:** Implement task-by-task and update checkboxes as work completes. Keep the dashboard static: command buttons copy CLI commands but do not write files.

**Goal:** Add dashboard command buttons that copy `research action set` commands for review actions.

**Architecture:** Extend `dashboard.py` review-action rendering with PowerShell-safe command generation, state metadata display, and clipboard JavaScript. Keep v0.16 state loading and sidecar schema unchanged.

**Tech Stack:** Python standard library, static HTML/CSS/JavaScript embedded by `dashboard.py`, `unittest`, existing release doctor.

---

## File Structure

- Modify: `src/taiwan_stock_analysis/dashboard.py`
- Modify: `tests/test_dashboard.py`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `docs/usage-workflow.md`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.17.0.md`
- Modify: `tests/test_cli.py`

---

## Task 1: Add Dashboard Command Rendering

- [ ] Add a PowerShell argument quoting helper.
- [ ] Add helper to resolve sibling `review_action_state.json` path for a research summary.
- [ ] Add helper to build `research action set` commands.
- [ ] Add command buttons for `done`, `deferred`, `ignored`, and `reopen`.
- [ ] Add a `commands` column to the Review Actions table.
- [ ] Render persisted `note` and `updated_at` metadata when present.
- [ ] Add data attributes needed for copy interaction.
- [ ] Add dashboard tests for command rendering, reopen mapping, state path, metadata, and escaping.
- [ ] Run `python -m unittest tests.test_dashboard -v`.
- [ ] Commit dashboard command rendering.

## Task 2: Add Clipboard JavaScript

- [ ] Add an isolated dashboard command-copy initializer.
- [ ] Prefer `navigator.clipboard.writeText`.
- [ ] Add fallback textarea plus `document.execCommand("copy")`.
- [ ] Add section-level copy status label.
- [ ] Keep commands inspectable in DOM if clipboard is blocked.
- [ ] Add tests for JavaScript hooks and fallback text.
- [ ] Run `python -m unittest tests.test_dashboard -v`.
- [ ] Commit clipboard behavior.

## Task 3: Documentation and Release Metadata

- [ ] Bump `pyproject.toml` to `0.17.0`.
- [ ] Update README version badge and feature list.
- [ ] Update `docs/usage-workflow.md` with dashboard command-copy workflow.
- [ ] Add `CHANGELOG.md` entry for `v0.17.0`.
- [ ] Create `docs/releases/v0.17.0.md`.
- [ ] Update release doctor CLI test to `0.17.0`.
- [ ] Run `python -m taiwan_stock_analysis.cli doctor release --version 0.17.0`.
- [ ] Commit docs and release metadata.

## Task 4: Final Verification

- [ ] Run focused dashboard and CLI tests.
- [ ] Run `python -m unittest discover -s tests -v`.
- [ ] Run Python 3.10 smoke with `uv run --python 3.10 python -m unittest discover -s tests -v`.
- [ ] Run release doctor for `0.17.0`.
- [ ] Run demo research workflow and verify dashboard command buttons/copy hooks appear.
- [ ] Confirm `git diff --check` and clean working tree.

## Release Handoff

After verification, present branch status and ask whether to merge to `main`, push, and tag `v0.17.0`.
